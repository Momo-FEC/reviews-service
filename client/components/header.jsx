import React from 'react';

var RatingSnapshot = (props) => {

  // var allReviewsLength = Object.keys(props.reviews).length;
  // var allReviewsLengthWith5Overall = Object.keys(props.review.overall).length;
  
  return (
    <div>
      
    </div>
  );
};

export default RatingSnapshot;